@model IEnumerable<WEB_Assignment_Team4.Models.Competition>

@if (Model.ToList().Count > 0)
{
    <div class="table-responsive">
        <table id="selectInterest" class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Name</th>
                    <th>Start Date</th>
                    <th>End Date</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var items in Model)
                {
                <tr>
                    <td>@items.Name</td>
                    <td>@items.StartDate</td>
                    <td>@items.EndDate</td>

                    <td>
                        <a asp-action="List" asp-controller="Competition"
                           asp-route-id="@items.CompetitionID">Select Competition</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <span style="color:red;">No Records Found!</span>
}
